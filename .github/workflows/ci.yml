# CI for integrations.
#
# tsc + unit tests on every PR / push to main. Tests here are pure-
# function (Plaid mapper, connector-interface conformance) — no DB
# required, no Plaid API hit.
#
# Production deployment is via Vercel's GitHub integration. See
# ledger-core/docs/DEPLOY.md.

name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Generate Prisma client
        run: npx prisma generate
      - name: TypeScript check
        run: npx tsc --noEmit
      - name: Unit tests
        run: npx vitest run
